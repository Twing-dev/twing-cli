{
  "name": "@twing/simulator",
  "version": "0.0.0",
  "private": true,
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "bin": {
    "twing-simulator": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/*.test.js"
  },
  "dependencies": {
    "@twing/core": "*",
    "@twing/cli": "*",
    "@twing/server": "*"
  }
}
